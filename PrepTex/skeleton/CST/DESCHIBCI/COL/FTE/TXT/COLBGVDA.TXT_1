      *DAD ini  12-NOV-1993 / 04-OCT-1993 / 04-AGO-1993   (Memo.28-06-93
      *Valida existencia de informacion en campos informados con perdida
       VAL-DAT-PREV-ACA SECTION.
       INI-VAL-DAT-PREV-ACA.
           MOVE SPACES TO MSG-COD-MENS.

           IF FIO-PROG NOT = 'COLPOOPC'
               GO TO CAN-VAL-DAT-PREV-ACA.

           IF OPC-FEC-FCOL IN OPC NOT > ZEROES OR
              OPC-FEC-FINP IN OPC NOT > ZEROES OR
              OPC-FEC-FING IN OPC NOT > ZEROES
               MOVE 'COL'          TO MSG-COD-SIST
               MOVE 'OPCFCOLNOINF' TO MSG-COD-MENS
               PERFORM GET-MSG.
      *ALD-BCI-INI
           IF OPC-VAL-SCRE IN OPC NOT > ZEROES
               MOVE 'COL'          TO MSG-COD-SIST
               MOVE 'OPCSCRENOINF' TO MSG-COD-MENS
               PERFORM GET-MSG.
      *ALD-BCI-FIN
           GO TO FIN-VAL-DAT-PREV-ACA.
       CAN-VAL-DAT-PREV-ACA.
           IF CAN-FEC-FCAN IN CAN NOT > ZEROES OR
              CAN-FEC-FOUT IN CAN NOT > ZEROES OR
              CAN-FEC-FEGR IN CAN NOT > ZEROES
               MOVE 'COL'          TO MSG-COD-SIST
               MOVE 'CANFCANNOINF' TO MSG-COD-MENS
               PERFORM GET-MSG.

           IF CAN-MSC-STAT IN CAN = 'A'        AND
              CAN-SGV-TPAG IN CAN NOT > ZEROES
               GO TO FIN-VAL-DAT-PREV-ACA.

           IF CAN-MSC-CANC IN CAN NOT = 'CIE'  AND
              TOC-MSC-OPEC IN TOC NOT = 'B'    AND
              VEN-VAL-CAPI IN VEN     > ZEROES AND
              RCV-SGV-CAPI IN RCV NOT > ZEROES
               MOVE 'COL'          TO MSG-COD-SIST
               MOVE 'RCVCAPINOINF' TO MSG-COD-MENS
               PERFORM GET-MSG.

           IF CAN-MSC-CANC IN CAN NOT = 'CIE'  AND
              TOC-MSC-OPEC IN TOC NOT = 'B'    AND
              RCV-SGV-FINA IN RCV NOT > ZEROES
               MOVE 'COL'          TO MSG-COD-SIST
               MOVE 'RCVFINANOINF' TO MSG-COD-MENS
               PERFORM GET-MSG.

           IF CAN-MSC-CANC IN CAN = 'CTT'      AND
              VEN-VAL-CAPI IN VEN NOT < ZEROES AND
              RCV-SGV-CAPI IN RCV NOT > ZEROES
               MOVE 'COL'          TO MSG-COD-SIST
               MOVE 'RCVCAPINOINF' TO MSG-COD-MENS
               PERFORM GET-MSG.

           IF CAN-MSC-CANC IN CAN = 'CTT'      AND
              VEN-VAL-CAPI IN VEN NOT < ZEROES AND
              RCV-SGV-FINA IN RCV NOT > ZEROES
               MOVE 'COL'          TO MSG-COD-SIST
               MOVE 'RCVFINANOINF' TO MSG-COD-MENS
               PERFORM GET-MSG.
       FIN-VAL-DAT-PREV-ACA.
           EXIT.
      *DAD fin  12-NOV-1993 / 04-OCT-1993 / 04-AGO-1993
