$	OPR = F$EDIT(GNS$OPR,"TRIM")
$	VRF = F$ENVIRONMENT("VERIFY_IMAGE")
$	SET NOVER
$!recibe el nombre del proceso + clave + nombre del reporte + reemplaza
$!	IF P2 .NES. "" THEN P1 = P1 + P2
$	ARC = "REP:''OPR'''P1'.REP"
$	EXT = ""
$	IF P4 .EQS. "N" THEN GOTO NRP
$	SEA = F$SEARC(ARC)
$	IF SEA .EQS. "" THEN EXT = ";1"
$	IF SEA .EQS. "" THEN GOTO NRP
$	@GNSCOM:FIL.COM 'SEA'
$	EXT = GNS$VER
$ NRP:
$	DEFINE/NOLOG 'P3' 'ARC''EXT'
$!	SHOW LOGICAL 'P3'
$	IF VRF .EQS. "TRUE"  THEN SET VER
