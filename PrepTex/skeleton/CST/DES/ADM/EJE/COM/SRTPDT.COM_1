$	IF F$MODE() .EQS. "BATCH" THEN SET NOVER
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo a sortear ? "
$	GOTO P1
$ MAIN:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	@GNSCOM:FIL.COM 'P1' "SRT"
$	SORT/STABLE/KEY=(POS:26,SIZE:3,NUMBER:1)/KEY=(POS:5,SIZE:3,NUMBER:2) 'P1' 'GNS$FIL'.SRT
