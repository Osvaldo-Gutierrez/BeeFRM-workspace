$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$ DIR:
$	DIR REP:*.REP;*
$	INQUIRE P1 "Archivo a mirar (SI NO DESEA NINGUNO DIGITE RETURN ) ? "
$	IF P1 .EQS. "" THEN GOTO FIN
$ MAIN:
$	IF F$SEARC("REP:''P1'.REP") .EQS. "" THEN WRITE -
		SYS$OUTPUT "[7mNO EXISTE ARCHIVO ''P1'.REP[0m"
$	IF F$SEARC("REP:''P1'.REP") .EQS. "" THEN GOTO DIR
$	SET TERMINAL/WIDTH=132
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	EDIT REP:'P1'.REP/READ
$	GOTO DIR
$ FIN:
$	SET TERMINAL/WIDTH=80
