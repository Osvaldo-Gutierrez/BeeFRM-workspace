           MOVE {fld} IN {buf} TO LOC-LUGA.
           PERFORM TAB-BUS-LOC.
*% IF ( LGL = 0 ) OR ( LGL > 12 )
           MOVE LOC-DESC TO {gls} IN {buf}.
*% ELSE
*% IF LGL > 5
           MOVE LOC-DCOR TO {gls} IN {buf}.
*% ELSE
           MOVE LOC-ABRV TO {gls} IN {buf}.
*% END
*% END
