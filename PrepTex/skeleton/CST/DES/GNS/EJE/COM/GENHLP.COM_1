$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	ON WARNING   THEN GOTO FIN
$	ON CONTROL_Y THEN GOTO FIN
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m",-
		"			Generando...[0m"
$	CPT = P2
$	@GNSCOM:FIL 'P1' DOC
$	PTH = GNS$PTH
$	FIL = GNS$FIL
$	P2  = FIL
$	RUN GNSEXE:GENHLP
$	@GNSCOM:DESLIM 'FIL' PRT
$	WRITE SYS$OUTPUT "[1m[A[K",-
	"			''FIL'.PRT ''FIL'.HLP Generados[0m"
$ FIN:
$	EXIT
