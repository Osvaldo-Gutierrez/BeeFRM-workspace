$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Programa ? "
$	GOTO P1
$ MAIN:
$	ON WARNING   THEN GOTO ERR
$	ON CONTROL_Y THEN GOTO ERR
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	@GNSCOM:FIL.COM  "''P1'" "PTX"
$	WRITE SYS$OUTPUT "[5m[1m",-
		"			Generando...[0m"
$	PAI = "ALL"
$	CLI = "ALL"
$	IF GNS$PAI .NES. "" THEN PAI = GNS$PAI
$	IF GNS$CLI .NES. "" THEN CLI = GNS$CLI
$	P2 = -
"(PGM_''GNS$SOP'=TRUE,PGM_''GNS$LNG'=TRUE,PGM_''GNS$AIO'=TRUE,PGM_''GNS$AIU'=TRUE,PGM_''PAI'=TRUE,PGM_''CLI'=TRUE,PGM_''GNS$VRS'=TRUE)"
$	TPO = F$EXTRACT(3,2,"''GNS$FIL'")
$	TP1 = F$EXTRACT(3,1,"''GNS$FIL'")
$	TP2 = F$EXTRACT(4,1,"''GNS$FIL'")
$	EXT = ""
$	IF TP1 .NES. "P" THEN EXT = "TXT"
$	IF EXT .NES. ""  THEN GOTO PTX
$ PGM:
$	IF TP2 .EQS. "A" .OR. TP2 .EQS. "Q" .OR. TP2 .EQS. "U" .OR. -
	   TP2 .EQS. "P" THEN GOTO PGI
$	IF TP2 .EQS. "B" .OR. TP2 .EQS. "G" .OR. TP2 .EQS. "L" .OR. -
	   TP2 .EQS. "F" THEN EXT = "COB"
$	IF EXT .EQS. "" THEN EXT = "TXT"
$	GOTO PTX
$ PGI:
$	IF GNS$AIO .EQS. "DTC" THEN EXT = "PGD"
$	IF GNS$AIO .EQS. "VSI" THEN EXT = "PGI"
$ PTX:
$	PTX 'GNS$PTH' 'GNS$FIL'.'EXT'/NOSHOW/UNDEFINED=ZERO/COMMAND='P2'/CONTROL="*%"
$	WRITE SYS$OUTPUT "[1m[A[K", -
		"			''GNS$FIL'.''EXT' Generado[0m"
