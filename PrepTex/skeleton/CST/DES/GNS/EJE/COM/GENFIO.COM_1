$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	ON WARNING   THEN GOTO ERR
$	ON CONTROL_Y THEN GOTO ERR
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m",-
		"			Generando...[0m"
$	@DESCOM:FIL 'P1' FDL
$	PTH = DES$PTH
$	FIL = DES$FIL
$	P2  = FIL
$	ASS FIO'P2'.LOG SYS$OUTPUT
$	ASS FIO'P2'.LOG SYS$ERROR
$	RUN DESEXE:GENFIO
$	RUN DESEXE:DESDOCARC
$ FIN:
$	SET NOON
$	DEASS SYS$OUTPUT
$	DEASS SYS$ERROR
$	DELETE/NOLOG/NOCONFIRM FIO'P2'.LOG.*
$	EXIT
$ ERR:
$	SET NOCONTROL=Y
$	SET NOON
$	DEASS SYS$OUTPUT
$	DEASS SYS$ERROR
$	TYPE FIO'P2'.LOG
$	SET CONTROL=Y
$	EXIT
