$	ON WARNING   THEN GOTO ERR
$	ON CONTROL_Y THEN GOTO ERR
$	IF P2 .NES. "" THEN P2 = "." + P2
$	SORT/workf=10/STABLE/KEY=(POS:1,SIZE:9) 'P1'.SRT 'P1'.SRT
$	OPEN/WRITE FILE 'P1'.COM
$	WRITE FILE "$ RUN GNSEXE:DESLIM"
$	WRITE FILE P1 + P2
$	WRITE FILE "$ EXIT"
$	CLOSE FILE
$	@'P1'.COM
$ FIN:
$	SET NOON
$	DELETE/NOLOG/NOCONFIRM 'P1'.SRT.*, 'P1'.COM.*
$	EXIT
$ ERR:
$	SET NOCONTROL=Y
$	SET NOON
$	CLOSE FILE
$	DELETE/NOLOG/NOCONFIRM 'P1'.SRT.*, 'P1'.COM.*
$	MALO
$	EXIT
