$	usn	= f$edit(f$getjpi(0,"username"),"TRIM")
$	gns$psw :==  ""
$	mes = F$ELEMENT(1,"-",F$TIME())
$	if mes .EQS. "JAN" then mes = "ENE"
$	if mes .EQS. "APR" then mes = "ABR"
$	if mes .EQS. "AUG" then mes = "AGO"
$	if mes .EQS. "DEC" then mes = "DIC"
$	if usn .eqs. "ISILVA"     then gns$psw :==  "SLV''mes'"
$	if usn .eqs. "JSEPULVEDA" then gns$psw :==  "JSP''mes'"
$	if usn .eqs. "MOLIVARES"  then gns$psw :==  "MLV''mes'"
$	if usn .eqs. "ICATALDO"   then gns$psw :==  "CTL''mes'"
$	if usn .eqs. "ALOPEZ"     then gns$psw :==  "LPZ''mes'"
$	if usn .eqs. "PGUZMAN"    then gns$psw :==  "PGZ''mes'"
$	if usn .eqs. "DAGUAYO"    then gns$psw :==  "DGY''mes'"
$	if usn .eqs. "JVALDES"    then gns$psw :==  "JVL''mes'"
$	if usn .eqs. "AARANCI"    then gns$psw :==  "RNC''mes'"
$	if usn .eqs. "FLIZANA"    then gns$psw :==  "FLZ''mes'"
$	if usn .eqs. "RVALDES"    then gns$psw :==  "RVL''mes'"
$	if gns$psw .nes. "" then exit
$!
$	SET TERMINAL/LOCAL_ECHO
$	write sys$output "[H [J"
$	write sys$output " "
$ reppsw:
$	on control_y then goto reppsw
$	on control_c then goto reppsw
$	INQUIRE gns$psw1 "Password MVS ? "
$	on control_y then goto reppsw
$	on control_c then goto reppsw
$	INQUIRE gns$psw2  "Verificacion ? "
$	if gns$psw1 .eqs. gns$psw2 then goto okpsw
$	write sys$output "[H [J"
$	write sys$output " "
$	write sys$output "[1m Error en verificacion de password [0m"
$	goto reppsw
$ okpsw:
$	gns$psw :== 'gns$psw2'
$	SET TERMINAL/NOLOCAL_ECHO
