$ P1:
$	IF P1 .NES. "" THEN GOTO VALP1
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$ INQP1:
$	INQUIRE P1 "Item (CLI,LUG,PRO,[7mACT[0m,ENT) ? "
$ VALP1:
$	IF P1 .EQS. "" THEN P1 = "ACT"
$	IF P1 .EQS. "CLI" .OR. P1 .EQS. "LUG" .OR. P1 .EQS. "PRO" .OR. -
           P1 .EQS. "ACT" .OR. P1 .EQS. "ENT" THEN GOTO P2
$	IF F$MODE() .NES. "BATCH" THEN GOTO INQP1
$	WRITE SYS$OUTPUT "Faltan parametros"
$	EXIT
$ P2:
$	IF P2 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$ INQP2:
$	INQUIRE P2 "Descripcion de Item ? "
$	IF P2 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .NES. "BATCH" THEN GOTO INQP2
$	WRITE SYS$OUTPUT "Faltan parametros"
$	EXIT
$ MAIN:
$	SEARC/MATCH=AND [CST.DES.ADM.EJE.PAR]ADMTAB.PAR " ''P1'",'P2'
