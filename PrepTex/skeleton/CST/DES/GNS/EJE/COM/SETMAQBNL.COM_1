$	usn = "''f$extract(0,8,f$getjpi(0,"username"))'"
$ a:
$	if usn .nes. "JSEPULVEDA" then goto c
$	gns$maq :==  "JSS-GENE"
$	gns$jnm :==  "JSS"
$	goto fin
$ c:
$	gns$maq :==  "USR-GENE"
$	gns$jnm :==  "GNS"
$ fin:
$	exit
