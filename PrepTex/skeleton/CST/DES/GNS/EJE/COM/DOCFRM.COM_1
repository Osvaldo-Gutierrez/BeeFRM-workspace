$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	ON ERROR	THEN GOTO ERR
$	ON WARNING	THEN GOTO ERR
$	ON CONTROL_Y	THEN GOTO ERR
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m",-
	"		Generando...[0m"
$	IF F$LOCATE("/F",P1) .NES. F$LENGTH(P1) THEN GOTO LIB
$	@GNSCOM:FIL 'P1'
$	P2 = GNS$FIL
$	GOTO CON
$ LIB:
$	P2 = F$EXTRACT(F$LOCATE("=",P1)+1,F$LENGTH(P1),P1)
$ CON:
$	FMS/DESCRIPTION 'P1'/OUTPUT='P2'.FLG
$	PTH = P2 + ".FLG"
$	RUN GNSEXE:DOCFRM
$	DELETE/NOLOG/NOCONFIRM	'P2'.FLG;
$ ASS EDIT.LOG SYS$OUTPUT
$ on warning   then goto ext
$ on control_y then goto ext
$	EDITT 'FRM'/COM=GNSCOM:DOCFRM'GNS$PAI''GNS$CLI'.EDT
$	DELETE/NOLOG 'FRM';-1
$	RENAME/NOLOG 'FRM' 'FRM';-1
$ ext:
$ DEASS SYS$OUTPUT
$ DELETE/NOLOG/NOCONF EDIT.LOG;
$	WRITE SYS$OUTPUT "[A[2K"
$	WRITE SYS$OUTPUT "[1m			",-
	"''FRM' Generado[0m"
$ ERR:
