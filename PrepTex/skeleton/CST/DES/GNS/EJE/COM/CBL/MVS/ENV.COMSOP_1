$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo(s) ? "
$	GOTO P1
$ MAIN:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	TIME = F$ELEMENT(1," ",F$TIME())
$	EXT = F$EXTRACT(0,2,TIME)+F$EXTRACT(3,2,TIME)+F$EXTRACT(6,2,TIME)+-
              F$EXTRACT(9,2,TIME)
$!	WRITE SYS$OUTPUT DES
$	TMP = F$TRNLNM("TMP")
$	IF TMP .EQS. "" THEN TMP = "[]"
$	GENJOB 'P1' 'TMP'ENV.'EXT'
$	RJE 'TMP'ENV.'EXT'
$	DELETE/LOG/NOCONFIRM 'TMP'ENV.'EXT';
