$ P1:
$	IF P1 .NES. "" THEN GOTO P2
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ P2:
$	IF P2 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P2 "VAX o VB ? "
$	GOTO P2
$ P3:
$	IF P3 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P3 "AMBIENTE (IBM/UYS) ? "
$	GOTO P3
$ MAIN:
$ 	IF P2 .NES. "VAX" .AND. P2 .NES. "VB" THEN WRITE SYS$OUTPUT "PARAMETRO GENERACION INVALIDO"
$ 	IF P2 .NES. "VAX" .AND. P2 .NES. "VB" THEN WRITE SYS$OUTPUT "SOLO ACEPTA VAX O VB"
$ 	IF P2 .NES. "VAX" .AND. P2 .NES. "VB" THEN EXIT
$! 	IF P3 .NES. "IBM" .AND. P3 .NES. "UYS" THEN WRITE SYS$OUTPUT "PARAMETRO GENERACION INVALIDO"
$! 	IF P3 .NES. "IBM" .AND. P3 .NES. "UYS" THEN WRITE SYS$OUTPUT "SOLO ACEPTA IBM O UYS"
$! 	IF P3 .NES. "IBM" .AND. P3 .NES. "UYS" THEN EXIT
$	P4 = P2
$ 	IF P4 .EQS. "VAX" THEN P2 = "GEN_VAX=TRUE"
$ 	IF P4 .NES. "VAX" THEN P2 = "GEN_VAX=FALSE"
$	IF P3 .NES. "IBM" THEN P3 = "GEN_UYS=TRUE" 
$	IF P3 .EQS. "IBM" THEN P3 = "GEN_IBM=TRUE" 
$ 	CC = "%"
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m		Generando...[0m"
$	@GNSCOM:FIL 'P1' PTX
$!	P3 = GNS$FIL 
$	OPEN/WRITE FILE AUXPTX.COM
$	WRITE FILE "$ PTX"
$	WRITE FILE "''GNS$PTH'" + " ''GNS$FIL'" + ".SRC" + "/CONTROL=''CC'/UNDEFINED=ZERO/NOSHOW/COMMAND=(''P2',''P3')"
$	CLOSE FILE
$	@AUXPTX.COM
$	WRITE SYS$COMMAND "[1m[A[K			Modulo ''GNS$FIL'.SRC Generado[0m"
