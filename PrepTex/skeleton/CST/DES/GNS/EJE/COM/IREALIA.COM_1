$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
	IF F$SEARC(P1) .NES. "" THEN GOTO CON
$	WRITE SYS$OUTPUT "%Error-Archivo : ''p1', Inexistente"
$	EXIT
$ CON:
$	IF F$TRNLNM("TMP") .NES. "" THEN T = F$TRNLNM("TMP")
$	IF F$TRNLNM("TMP") .EQS. "" THEN T = "[]"
$	@GNSCOM:FIL 'P1' COB
$	@GNSCOM:COBSUB.COM "''GNS$PTH'" "R" "''T'" "''GNS$SOP'" "''P2'"
$	EXIT
