$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Biblioteca ? "
$	GOTO P1
$ MAIN:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	IF P2 .NES. "" THEN P2 = "/ONLY=" + P2
$	LIB/FULL/LIST'P3''P4''P5''P6''P7''P8' 'P1''P2'
