$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	DUP 'P1'
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	IF GNS$EXT .EQS. "EXE" THEN EXIT
$	L = F$LENGH(P1)
$	IF F$LOCATE("*",P1) .NE. L THEN EXIT
$	IF F$LOCATE("%",P1) .NE. L THEN EXIT
$	PUTALL 'P1'
