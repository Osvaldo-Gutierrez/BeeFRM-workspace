$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Numero en octal ? "
$	GOTO P1
$ MAIN:
$	LAR=F$LENGTH("''P1'")
$	I = LAR
$	J = 0
$	DEC = 0
$ LUP:
$	IF I .EQS. 0 THEN GOTO SHW
$	PTN = 1
$	K = 0
$ LUP2:
$	IF K .EQS. J THEN GOTO SUM
$	PTN = PTN*8
$	K = K + 1
$	GOTO LUP2
$ SUM:
$	DEC = DEC + F$EXTRACT(I-1,1,"''P1'")*PTN
$	I = I - 1
$	J = J + 1
$	GOTO LUP
$ SHW:
$	WRITE SYS$OUTPUT DEC
