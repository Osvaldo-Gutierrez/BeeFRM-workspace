$ P1:
$	IF P1 .NES. "" THEN GOTO P2
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo de Salida ? "
$	GOTO P1
$ P2:
$	IF P2 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P2 "Comando ? "
$	GOTO P2
$ MAIN:
$	@GNSCOM:FIL 'P1' "COM"
$	ASS 'GNS$FIL'.'GNS$EXT' SYS$OUTPUT
$	'P2'
$	DEASS SYS$OUTPUT
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	EDIT 'GNS$FIL'.'GNS$EXT'
$	EXIT
