$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	ON ERROR	THEN GOTO ERR
$	ON WARNING	THEN GOTO ERR
$	ON CONTROL_Y	THEN GOTO ERR
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m",-
	"		Generando...[0m"
$	@GNSCOM:FIL 'P1'
$!	ASS MAP'P3'.LOG SYS$OUTPUT
$!	ASS MAP'P3'.LOG SYS$ERROR
$	RUN GNSEXE:SACATAB
$ FIN:
$	SET NOON
$!	DEASS SYS$OUTPUT
$!	DEASS SYS$ERROR
$	EXIT
$ ERR:
$	SET NOCONTROL=Y
$	SET NOON
$!	DEASS SYS$OUTPUT
$!	DEASS SYS$ERROR
$	WRITE SYS$OUTPUT "[1m[A[K ERROR[0m"
$!	TYPE MAP'P3'.LOG
$	SET CONTROL=Y
