$	VRF = F$ENVIRONMENT("VERIFY_IMAGE")
$	SET NOVER
$! ASSIGNA EL ARCHIVO PARA EL REPORTE
$	V = ""
$	IF P3 .EQS. "N" THEN GOTO ASSRPT
$	R = F$SEARC("REP:''GNS$OPR'''P1'.REP")
$	IF R .EQS. "" THEN GOTO ASSRPT
$	V = F$PARSE(R,,,"VERSION")
$ ASSRPT:
$	ASS REP:'GNS$OPR''P1'.REP'V' 'P2'
$	IF VRF .EQS. "TRUE"  THEN SET VER
