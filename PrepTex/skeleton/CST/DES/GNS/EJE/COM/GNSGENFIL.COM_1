$ P1:
$	IF P1 .NES. "" THEN GOTO P2
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo a generar ( MNU, MN1, MN2, RPF, o BAT ) ? "
$	GOTO P1
$ P2:
$	IF P2 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P2 "Sistema ? "
$	GOTO P2
$ MAIN:
$	SKL = "GNSSRCAIO:GNS''P1'''GNS$USR'.SRCAIO"
$	IF F$SEARCH("''SKL'") .EQS. "" THEN GOTO NOSKL
$	WRITE SYS$OUTPUT "[5m[1m",-
		"			Generando...[0m"
$	OPEN/WRITE FILE GNSGENFIL.EDT
$	WRITE FILE "S/{MAQ}/''GNS$MAQ'/W/NOTYPE"
$	WRITE FILE "S/{CMQ}/''GNS$CMQ'/W/NOTYPE"
$	WRITE FILE "S/{SIS}/''P2'/W/NOTYPE"
$	WRITE FILE "S/{ctl}/SYSWKV/W/NOTYPE"
$	WRITE FILE "EXIT"
$	CLOSE FILE
$	ASS 'P1'.LOG SYS$OUTPUT
$	ASS 'P1'.LOG SYS$ERROR
$	EDIT 'SKL'/COMM=GNSGENFIL.EDT/OUT='P2''P1''GNS$USR'.COM
$	DEASS SYS$OUTPUT
$	DEASS SYS$ERROR
$	DELETE GNSGENFIL.EDT;*
$	DELETE 'P1'.LOG;*
$	WRITE SYS$COMMAND "[A[2K"
$	WRITE SYS$COMMAND "[1m			",-
		P2,P1,GNS$USR,".COM Generado[0m"
$	EXIT
$ NOSKL:
$	WRITE SYS$COMMAND "[1m			",-
		"No existe esqueleto[0m"
