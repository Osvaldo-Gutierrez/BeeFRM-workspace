$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	ON WARNING   THEN GOTO ERR
$	ON CONTROL_Y THEN GOTO ERR
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m",-
		"			Generando...[0m"
$	@DESCOM:FIL 'P1'
$	P2 = DES$FIL
$	ASS FRM'P2'.LOG SYS$OUTPUT
$	ASS FRM'P2'.LOG SYS$ERROR
$	FMS/DESC 'P1'/OUTPUT='P2'.FLG
$	PTH = P2 + ".FLG"
$	RUN DESEXE:GENFRM
$	FMSINS 'P1'
$ FIN:
$	SET NOON
$	DELETE/NOLOG/NOCONFIRM	'P2'.FLG.*
$ 	DEASS SYS$OUTPUT
$ 	DEASS SYS$ERROR
$	DELETE/NOLOG/NOCONFIRM	FRM'P2'.LOG.*
$	EXIT
$ ERR:
$	SET NOCONTROL=Y
$	SET NOON
$	CLOSE FILE
$	DELETE/NOLOG/NOCONFIRM	'P2'.FLG.*
$ 	DEASS SYS$OUTPUT
$ 	DEASS SYS$ERROR
$	WRITE SYS$OUTPUT "[1m[A[K ERROR[0m"
$	TYPE FRM'P2'.LOG
$	SET CONTROL=Y
$	EXIT
