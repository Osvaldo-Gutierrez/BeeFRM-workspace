$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Reporte ? "
$	GOTO P1
$ MAIN:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	FMS/LIBRARY/DELETE/LOG RPT/FORM_NAME='P1'
$!	LIBDEL GEN RPT%%%'P1'
$!	DELETE/NOLOG/NOCONF SOP:'GNS$PRY'R%'P1'
