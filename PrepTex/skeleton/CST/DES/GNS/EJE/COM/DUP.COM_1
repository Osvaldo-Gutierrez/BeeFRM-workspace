$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	@GNSCOM:BUSFIL 'P1'
$	IF GNS$ARC .EQS. "" THEN GOTO BLK
$	WRITE SYS$OUTPUT "Archivo:[7m''GNS$ARC'[0m"
$! FLB	IF GNS$UBI .EQS. "[]" THEN EXIT
$	IF GNS$UBI .EQS. "[]" THEN GOTO NOCOPY
$	COPY/NOLOG 'GNS$ARC' []
$! FLB
$ NOCOPY:
$	@GNSCOM:FIL "''GNS$ARC'"
$!
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	IF GNS$EXT .EQS. "EXE" THEN EXIT
$	L = F$LENGH(P1)
$	IF F$LOCATE("*",P1) .NE. L THEN EXIT
$	IF F$LOCATE("%",P1) .NE. L THEN EXIT
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	C = "EDIT"
$	IF GNS$EXT .EQS. "FDL" THEN C = "FDL"
$	'C' 'GNS$FIL'.'GNS$EXT'
$	EXIT
$ BLK:
$	WRITE SYS$OUTPUT "[7mNO encontre Archivo:''P1'[0m"
