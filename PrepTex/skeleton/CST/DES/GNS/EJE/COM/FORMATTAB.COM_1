$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	ON WARNING   THEN GOTO ERR
$	ON CONTROL_Y THEN GOTO ERR
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m",-
		"			Generando...[0m"
$	@gnsCOM:FIL 'P1' FDL
$	@[cst.jsepulve]genflag.com 'p1'
$	@gnscom:sacatab            'gns$fil'.fla
$	editt/command=e.e          'gns$fil'.out
$	convert/create/pad=%d32/fdl=[cst.des.gns.eje.fdl]tab.fdl 'gns$fil'.out -
	'gns$fil'.mae
$	delete/nolog 'gns$fil'.fla;
$	delete/noconfirm 'gns$fil'.out;
