$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Ingrese identificador en la forma [x,y] ? "
$	GOTO P1
$ MAIN:
$	GRP = F$ELEMENT(0,",","''P1'")
$	GRP = F$ELEMENT(1,"[","''GRP'")
$	MBR = F$ELEMENT(1,",","''P1'")
$	MBR = F$ELEMENT(0,"]","''MBR'")
$	DEC = (%O'MBR' + (%X10000*%O'GRP'))
$	SHOW SYMBOL DEC
