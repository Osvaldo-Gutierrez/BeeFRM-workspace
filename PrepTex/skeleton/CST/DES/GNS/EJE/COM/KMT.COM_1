$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	@GNSCOM:FIL 'P1' "TXT"
$	WRITE SYS$OUTPUT "[1mEnviando el archivo ''P1'[0m"
$	KERMIT := "$GNSEXE:KERMIT"
$	KERMIT SEND 'p1'
