$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Nombre Logico Biblioteca ? "
$	GOTO P1
$ MAIN:
$	F = ""
$	P = P1
$	IF P2 .EQS. "" THEN GOTO CONT
$	F = "/FORM_NAME=" + P2
$	P = P2
$ CONT:
$!	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	FMS/DESCRIPTION/LOG/DISPLAY=NOESCAPE_SEQUENCE/-
		OUTPUT='P'.LIS 'P1''F'
$	PRINT 'P'.LIS/DELETE
