$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	P3 = ""
$	P2 = F$TRNLNM("PAN")
$	IF P2 .NES "" THEN P3 = ", Asume : " + P2
$	INQUIRE P1 "Archivo PANEL''P3' ? "
$	IF P1 .EQS. "" THEN P1 = P2
$	GOTO P1
$ MAIN:
$	DEFINE/NOLOG PAN 'P1'
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	ON WARNING THEN GOTO EXT
$ LUP:
$	RUN GNSEXE:GNSPPPAN
$	GOTO LUP
$ EXT:
$	WRITE SYS$OUTPUT "(B[0m"
