$! SETPSWCST : SETEA PASSWORD DE USR CONSIST (PARA JCL MVS)
$	usn	= f$edit(f$getjpi(0,"username"),"TRIM")
$	gns$psw :==  ""
$	mes = F$ELEMENT(1,"-",F$TIME())
$	ano = F$EXTRACT(2,2,F$ELEMENT(2,"-",F$TIME()))
$ exit
$	if mes .EQS. "JAN" then mes = "ENE"
$	if mes .EQS. "APR" then mes = "ABR"
$	if mes .EQS. "AUG" then mes = "AGO"
$	if mes .EQS. "DEC" then mes = "DIC"
$	if usn .eqs. "ISILVA"      then gns$psw :==  "SLV''ano'''mes'"
$	if usn .eqs. "PVARGAS"     then gns$psw :==  "PVR''ano'''mes'"
$	if usn .eqs. "FLIZANA"     then gns$psw :==  "FLZ''ano'''mes'"
$	if usn .eqs. "JRIQUELME"   then gns$psw :==  "JRF''ano'''mes'"
$!      if usn .eqs. "PRYARG"      then gns$psw :==  "ARG''ano'''mes'" 
$       if usn .eqs. "MHERRERA"    then gns$psw :==  "MHM''ano'''mes'" 
$       if usn .eqs. "MHERRERA2"   then gns$psw :==  "MH2''ano'''mes'" 
$       if usn .eqs. "RECHEVER"    then gns$psw :==  "REL''ano'''mes'" 
$	if usn .eqs. "FTVAXUYS"    then gns$psw :==  "FTV''ano'''mes'"
$	if usn .eqs. "SYSTEM"      then gns$psw :==  "SYS''ano'''mes'"
$	if usn .eqs. "LOSORIO"     then gns$psw :==  "LOP''ano'''mes'"
$	if usn .eqs. "AFRI"        then gns$psw :==  "AFR''ano'''mes'"
$	if usn .eqs. "PVENEGAS"    then gns$psw :==  "PVE''ano'''mes'"
$	if usn .eqs. "CIMADES"     then gns$psw :==  "CIM''ano'''mes'"
$	if usn .eqs. "MPALMA"      then gns$psw :==  "MPB''ano'''mes'"
$	if usn .eqs. "AINAREJO"    then gns$psw :==  "AIB''ano'''mes'"
$	if usn .eqs. "RROMERO"     then gns$psw :==  "RRM''ano'''mes'"
$	if usn .eqs. "OPERATOR"    then gns$psw :==  "OPR''ano'''mes'"
$	if gns$psw .nes. "" then exit
$!
$	SET TERMINAL/LOCAL_ECHO
$	write sys$output "[H [J"
$	write sys$output " "
$ reppsw:
$	on control_y then goto reppsw
$	on control_c then goto reppsw
$	INQUIRE gns$psw1 "Password MVS ? "
$	on control_y then goto reppsw
$	on control_c then goto reppsw
$	INQUIRE gns$psw2  "Verificacion ? "
$	if gns$psw1 .eqs. gns$psw2 then goto okpsw
$	write sys$output "[H [J"
$	write sys$output " "
$	write sys$output "[1m Error en verificacion de password [0m"
$	goto reppsw
$ okpsw:
$	gns$psw :== 'gns$psw2'
$	SET TERMINAL/NOLOCAL_ECHO
