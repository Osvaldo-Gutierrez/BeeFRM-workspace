$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	ON WARNING   THEN EXIT
$	ON CONTROL_Y THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO P2
$	INQUIRE P1 "Reporte ? "
$	GOTO P1
$ P2:
$	IF P2 .EQS. "" THEN GOTO DEF
$	IF F$LOCATE("/",P2) .NE. F$LENGTH(P2) THEN GOTO MAIN
$	@GNSCOM:FIL 'P2' RPT
$	P2 = GNS$PTH
$	GOTO MAIN
$ DEF:
$	P2 = "GNSRPT/FORM_NAME=GNSRPT"
$ MAIN:
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	FMS/EDIT 'P2'/OUTPUT='P1'.RPT
$	EXIT
