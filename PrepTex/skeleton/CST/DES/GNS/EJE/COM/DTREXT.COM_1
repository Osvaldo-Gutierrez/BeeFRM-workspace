$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO P2
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Nodo ? "
$	GOTO P1
$ P2:
$	P2 = P1
$	L = F$LENGH(P2)
$ I2:
$	I = F$LOCATE(".",P2)
$	IF I.EQ.L THEN GOTO MAIN
$	I = I + 1
$	L = L - I
$	P2 = F$EXTRACT(I,L,P2)
$	GOTO I2
$ MAIN:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	DTR EXTRACT 'P1' ON 'P2'.DDL
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	EDIT 'P2'.DDL
