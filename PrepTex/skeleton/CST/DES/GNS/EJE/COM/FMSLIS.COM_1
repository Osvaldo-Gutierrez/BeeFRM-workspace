$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN GOTO MAIN
$	INQUIRE P1 "Formulario ? "
$ MAIN:
$	F = ""
$	P = "FMS"
$	IF P1 .EQS. "" THEN GOTO CONT
$	F = "/FORM_NAME=" + P1
$	P = P1
$ CONT:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	FMS/DESCRIPTION/LOG/DISPLAY=NOESCAPE_SEQUENCE/OUTPUT='P'.LIS FMS'F'
$	PRINT 'P'.LIS/DELETE
