$	IF F$MODE() .EQS. "BATCH" THEN P2 = "N"
$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	PFJ = "Archivo:[7m"
$	SFJ = "[0m"
$	IF F$MODE() .EQS. "BATCH" THEN PFJ = ""
$	IF F$MODE() .EQS. "BATCH" THEN SFJ = ""
$	@GNSCOM:BUSFIL 'P1' /ALL
$	IF GNS$ARC .EQS. "" THEN GOTO BLK
$	WRITE SYS$OUTPUT "''PFJ'''GNS$ARC'''SFJ'"
$	IF P2 .EQS. "" THEN INQUIRE/NOPUNC P2 "Edito (*/N) ? "
$	IF P2 .EQS. "N" THEN EXIT
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	EDIT 'GNS$ARC' /READ
$	EXIT
$ BLK:
$	WRITE SYS$OUTPUT "NO encontre ''PFJ'''P1'''SFJ'"
