$	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN GOTO MAIN
$	INQUIRE P1 "Reporte ? "
$ MAIN:
$	F = ""
$	P = "RPT"
$	IF P1 .EQS. "" THEN GOTO CONT
$	F = "/FORM_NAME=" + P1
$	P = P1
$ CONT:
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	FMS/DESCRIPTION/LOG/DISPLAY=NOESCAPE_SEQUENCE/OUTPUT='P'.LIS RPT'F'
$	PRINT 'P'.LIS/DELETE
