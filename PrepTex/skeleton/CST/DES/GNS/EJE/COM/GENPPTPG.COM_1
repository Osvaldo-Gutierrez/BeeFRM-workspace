$ P1:
$	IF P1 .NES. "" THEN GOTO P2
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P1 "Programa ? "
$	GOTO P1
$ P2:
$	IF P2 .NES. "" THEN GOTO MAIN
$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$	INQUIRE P2 "IBM o UYS ? "
$	GOTO P2
$ MAIN:
$ 	IF P2 .NES. "IBM" .AND. P2 .NES. "UYS" THEN WRITE SYS$OUTPUT "parametro generacion invalido"
$ 	IF P2 .NES. "IBM" .AND. P2 .NES. "UYS" THEN WRITE SYS$OUTPUT "Solo acepta IBM o UYS
$ 	IF P2 .NES. "IBM" .AND. P2 .NES. "UYS" THEN EXIT
$ 	IF P2 .EQS. "IBM" THEN P3 = "GEN_IBM=TRUE"
$ 	IF P2 .NES. "IBM" THEN P3 = "GEN_IBM=FALSE"
$ 	IF P2 .EQS. "IBM" THEN EXT = "PGI"
$ 	IF P2 .EQS. "UYS" THEN EXT = "PGI"
$ 	CC = "%"
$	IF F$MODE() .EQS. "BATCH" THEN 'BAT'
$	WRITE SYS$OUTPUT "[5m[1m		Generando...[0m"
$	@GNSCOM:FIL 'P1' PTX
$	OPEN/WRITE FILE AUXPTX.COM
$	WRITE FILE "$ PTX"
$	WRITE FILE "''GNS$PTH'" + " ''GNS$FIL'" + ".''EXT'" + "/CONTROL=''CC'/UNDEFINED=ZERO/NOSHOW/COMMAND=(''P3')"
$	CLOSE FILE
$	@AUXPTX.COM
$	DEL/NOLOG AUXPTX.COM.*
$	WRITE SYS$output "[1m[A[K			Modulo ''GNS$FIL'.''EXT' Generado[0m"
