      *Modulo generico para hacer UNSTRING
       GNS-UNS-STR SECTION.
       INI-GNS-UNS-STR.
           MOVE SPACES TO UNS-ELEM.
           MOVE 0 TO UNS-IELE
           MOVE 0 TO UNS-ISTR.
           MOVE 0 TO UNS-CONT.
       LUP-GNS-UNS-STR.
           ADD 1 TO UNS-ISTR.
           IF UNS-ISTR > UNS-IMAX
               IF UNS-NELE = UNS-CONT
                   GO TO FIN-GNS-UNS-STR
               ELSE
                   MOVE UNS-DLMT TO UNS-ELEM
                   GO TO FIN-GNS-UNS-STR.

           IF UNS-SCHR(UNS-ISTR) = UNS-DLMT
               IF UNS-NELE = UNS-CONT
                   GO TO FIN-GNS-UNS-STR
               ELSE
                   ADD 1 TO UNS-CONT
           ELSE
           IF UNS-NELE = UNS-CONT
               ADD 1 TO UNS-IELE
               MOVE UNS-SCHR(UNS-ISTR) TO UNS-ECHR(UNS-IELE).
           GO TO LUP-GNS-UNS-STR.
       FIN-GNS-UNS-STR.
           EXIT.
