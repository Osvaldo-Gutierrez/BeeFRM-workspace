      *Modulo interactivo que busca IDD.
       GNS-BUS-IDD SECTION.
       INI-GNS-BUS-IDD.
           IF SCR-QIDD > SPACES
               EXEC CICS READQ TS
                         QUEUE(SCR-QIDD)
                         INTO(IDD-REGI)
                         LENGTH(SCR-LIDD)
                         ITEM(1)
               END-EXEC.
       FIN-GNS-BUS-IDD.
           EXIT.
