      * FUNCIONES DE UNSTRING Y STRING
       STR-UNS SECTION.
       INI-STR-UNS.
           MOVE ZEROES TO STR-IPTR.
           MOVE SPACES TO STR-GLS-GUNS.
           PERFORM UNS-FIL VARYING STR-IFIL FROM 1 BY 1 UNTIL
                   STR-IFIL > 4.
       FIN-STR-UNS.
           EXIT.

       UNS-FIL SECTION.
       INI-UNS-FIL.
           PERFORM UNS-COL VARYING STR-ICOL FROM 1 BY 1 UNTIL
                   STR-ICOL > 40.
       FIN-UNS-FIL.
           EXIT.

       UNS-COL SECTION.
       INI-UNS-COL.
           ADD 1 TO STR-IPTR.
           IF STR-GLS-ESTR IN STR-GLS-GSTR(STR-IPTR) = STR-IND-DELI
               MOVE 41 TO STR-ICOL
           ELSE
               MOVE STR-GLS-ESTR IN STR-GLS-GSTR(STR-IPTR) TO
                    STR-GLS-EUNS IN STR-GLS-GUNS(STR-IFIL, STR-ICOL).
       FIN-UNS-COL.
           EXIT.

       STR-STR SECTION.
       INI-STR-STR.
           MOVE ZEROES TO STR-IPTR.
           MOVE SPACES TO STR-GLS-GSTR.
           PERFORM STR-FIL VARYING STR-IFIL FROM 1 BY 1 UNTIL
                   STR-IFIL > 4.
       FIN-STR-STR.
           EXIT.

       STR-FIL SECTION.
       INI-STR-FIL.
           PERFORM STR-COL VARYING STR-ICOL FROM 1 BY 1 UNTIL
                   STR-ICOL > 40.
       FIN-STR-FIL.
           EXIT.

       STR-COL SECTION.
       INI-STR-COL.
           IF STR-GLS-EUNS IN STR-GLS-GUNS(STR-IFIL, STR-ICOL)
              NOT = SPACES
               ADD 1 TO STR-IPTR
               MOVE 'N' TO STR-IND-ISTR
               MOVE STR-GLS-EUNS IN STR-GLS-GUNS(STR-IFIL, STR-ICOL)
                    TO STR-GLS-ESTR IN STR-GLS-GSTR(STR-IPTR)
           ELSE
               IF STR-IND-ISTR = 'N'
                   ADD 1 TO STR-IPTR
                   MOVE 'S' TO STR-IND-ISTR
                   MOVE ' ' TO STR-GLS-ESTR IN STR-GLS-GSTR(STR-IPTR).
       FIN-STR-COL.
           EXIT.

