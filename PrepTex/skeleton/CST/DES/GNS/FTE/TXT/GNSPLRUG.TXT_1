*%! Codigo Adicional GNSPLRUG
*%!
*% IF WSS
      *<<<< WSS
      *Entidades de TAB
       COPY TABBRUSR.
      *>>>>
*% END
*% IF WSS_DTC
      *<<<< WSS_DTC
      *Entidades de TAB
       COPY GNSWGRQA REPLACING ==ADR-REQA== BY ==ADR-USR-REQA==.
      *>>>>
*% END
*% IF LUP_OUT
      *<<<< LUP_OUT

      *BUS-TAB Busca Glosa de codigo en tabla
           IF RUG-COD-GRPO IN SRT > SPACES
               MOVE 'GNS' TO TAB-COD-SIST
               MOVE 'GRP' TO TAB-COD-TTAB IN TAB
               MOVE RUG-COD-GRPO IN SRT TO TAB-COD-CTAB IN TAB
               PERFORM BUS-TAB
               MOVE TAB-GLS-DESC IN TAB TO WSS-GLS-GRPO IN WSS-RUG
           ELSE
               MOVE SPACES TO WSS-GLS-GRPO IN WSS-RUG.

      *BUS-USR busca usuario en tablas
           IF RUG-COD-USER IN SRT > SPACES
               MOVE RUG-COD-USER IN SRT TO USR-COD-USER IN USR
               PERFORM BUS-USR
               IF MSG-COD-MENS = SPACES
                  MOVE USR-GLS-DESC IN USR TO WSS-GLS-USER IN WSS-RUG
               ELSE
                  MOVE SPACES TO WSS-GLS-USER IN WSS-RUG
           ELSE
               MOVE SPACES TO WSS-GLS-USER IN WSS-RUG.
      *>>>>
*% END
*% IF EOF
      *<<<<
      *Entidades de TAB
       COPY TABBBUSR.
       COPY TABBFUSR.
      *Entidades de GNS
       COPY GNSBBTAB.
       COPY GNSBBMSG.
      *>>>>
*% END
