% IF NOT CAMPO
              05 FILLER     AT {fila}, {colu} VALUE 
                 "{txto}".
% EXIT
% END
              05 SC-{rreg}-{tipo}-{nomb} AT {fila}, {colu}
                            PIC X({larg})
% IF DISP
%   IF NUMERO
                            FROM {rreg}-{tipo}-{nomb}-ALF IN {frm}-FLD.
%   ELSE
                            FROM {rreg}-{tipo}-{nomb} IN {frm}-FLD.
%   END
% ELSE
%   IF NUMERO
                            USING {rreg}-{tipo}-{nomb}-ALF IN {frm}-FLD
%   ELSE
                            USING {rreg}-{tipo}-{nomb} IN {frm}-FLD
%   END
                            SHADOWED BY {rreg}-{tipo}-{nomb}-ATR IN {frm}-FLD.
% END
