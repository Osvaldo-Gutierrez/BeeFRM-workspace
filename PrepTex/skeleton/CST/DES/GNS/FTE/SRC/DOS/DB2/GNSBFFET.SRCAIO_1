
       {PRF}-F{reg}{ncur} SECTION.
       INI-{PRF}-F{reg}{ncur}.
      *Get next or Get previous
           EXEC SQL FETCH {reg}{ncur} INTO
*% INCLUDE 'GNSSRCAIO:GNSBFFLD.SRCAIO'
           END-EXEC.
       FIN-{PRF}-F{reg}{ncur}.
           EXIT.
