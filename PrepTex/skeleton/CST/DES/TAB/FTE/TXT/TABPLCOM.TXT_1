*%! Codigo adicional
*% IF WSS_DTC
      *<<<< WSS_DTC
       COPY GNSWGRQA REPLACING ==ADR-REQA== BY ==ADR-USR-REQA==.
      * COPY GNSWGRQA REPLACING ==ADR-REQA== BY ==ADR-MSG-REQA==.
      * COPY GNSWGRQA REPLACING ==ADR-REQA== BY ==ADR-TAB-REQA==.
      *>>>>
*% END
*% IF WSS
      *<<<< WSS
      * COPY GNSBRTAB.
      * COPY GNSBRMSG.
       COPY TABBRUSR.
      *>>>>
*% END
*% IF INI_FST_INP
      *<<<< INI_FST_INP
           IF NOT FIO-STAT-OKS
               DISPLAY 'ERROR DE APERTURA EN ARCHIVO COM ' FIO-STAT
               PERFORM GNS-PRO-END.
           GO TO INI-OUT-SRT.
      *>>>>
*% END
*% IF INI_OUT
      *<<<< INI_OUT
           MOVE LOW-VALUES TO COM.
           MOVE FIO-GET-NLS TO FIO-CMND.
           PERFORM TAB-FIO-COM.
           IF NOT FIO-STAT-OKS OR
              COM-COD-TTAB IN COM NOT = 'COM' OR
              COM-COD-COMU IN COM > SPACES
               MOVE 'TAB'        TO MSG-COD-SIST
               MOVE 'TAB    NEX' TO MSG-COD-MENS
               PERFORM GET-MSG
               DISPLAY MSG-GLS-DESC
               PERFORM GNS-PRO-END.
           MOVE COM-GLS-DESC IN COM TO WSS-GLS-DTAB.
           IF COM-COD-ATRN IN COM > SPACES
               MOVE FIO-INP TO FIO-CMND
               PERFORM TAB-FIO-USR
               MOVE COM-COD-ATRN IN COM TO USR-COD-USER IN USR
               PERFORM BUS-USR
               IF NOT FIO-STAT-OKS
                   MOVE '00'   TO FIO-STAT
                   MOVE SPACES TO USR-GLS-DESC IN USR
               ELSE
                   NEXT SENTENCE      
           ELSE
               MOVE SPACES TO USR-GLS-DESC.
           MOVE FIO-GET-NXT TO FIO-CMND.
           PERFORM TAB-FIO-COM.
      *>>>>    
*% END 
*% IF LUP_INP
      *<<<< LUP_INP
           IF COM-COD-TTAB > 'COM'
               GO TO FIN-INP-SRT.
      *>>>>
*% END
*% IF LUP_OUT
      *<<<< LUP_OUT 
           IF COM-COD-DPTO > SPACES
               MOVE 'TAB'               TO TAB-COD-SIST
               MOVE 'DPT'               TO TAB-COD-TTAB
               MOVE COM-COD-DPTO IN COM TO TAB-COD-CTAB
               PERFORM BUS-TAB
               MOVE TAB-GLS-DESC        TO WSS-GLS-DPTO
           ELSE
               MOVE SPACES TO WSS-GLS-DPTO.
           IF COM-COD-PRVC > SPACES
               MOVE 'TAB'        TO TAB-COD-SIST
               MOVE 'PRV'        TO TAB-COD-TTAB
               MOVE COM-COD-PRVC TO TAB-COD-CTAB
               PERFORM BUS-TAB
               MOVE TAB-GLS-DESC TO WSS-GLS-PRVC
           ELSE
               MOVE SPACES TO WSS-GLS-PRVC.
           IF COM-COD-REGI > SPACES
               MOVE 'TAB'               TO TAB-COD-SIST
               MOVE 'REG'               TO TAB-COD-TTAB
               MOVE COM-COD-REGI IN COM TO TAB-COD-CTAB
               PERFORM BUS-TAB
               MOVE TAB-GLS-DESC        TO WSS-GLS-REGI
           ELSE
               MOVE SPACES TO WSS-GLS-REGI.
           IF COM-COD-PAIS > SPACES
               MOVE 'TAB'               TO TAB-COD-SIST
               MOVE 'PAI'               TO TAB-COD-TTAB
               MOVE COM-COD-PAIS IN COM TO TAB-COD-CTAB
               PERFORM BUS-TAB
               MOVE TAB-GLS-DESC        TO WSS-GLS-PAIS
           ELSE
               MOVE SPACES TO WSS-GLS-PAIS.
      *>>>>
*% END
*% IF EOF
      *<<<< EOF
      * COPY GNSBFTAB.
      * COPY GNSBFMSG.
       COPY TABBFUSR.
       COPY TABBBUSR.
       COPY GNSBBTAB.
       COPY GNSBBMSG.
      *>>>>
*% END
