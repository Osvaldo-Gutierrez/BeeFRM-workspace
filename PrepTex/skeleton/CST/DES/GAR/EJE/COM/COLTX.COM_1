$	IF F$MODE() .EQS. "BATCH" THEN EXIT
$ 	ON WARNING THEN EXIT
$ P1:
$	IF P1 .NES. "" THEN GOTO MAIN
$	INQUIRE P1 "Archivo ? "
$	GOTO P1
$ MAIN:
$	DEFINE/NOLOG SYS$INPUT SYS$COMMAND
$	EDIT COLTXT:COL'P1'.TXT/OUTPUT=[]COL'P1'.TXT
